from collections import Counter

def build_vocab(sentences):
    counter = Counter()
    for s in sentences:
        counter.update(s)
    
    word_to_id = {w:i for i,(w,c) in enumerate(counter.items())}
    id_to_word = {i:w for w,i in word_to_id.items()}

    return word_to_id, id_to_word